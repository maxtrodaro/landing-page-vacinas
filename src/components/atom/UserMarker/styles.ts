import styled from 'styled-components';

export const MarkerContainer = styled.section`
  display: block;
  width: 20px;
  height: 30px;

  svg {
    width: 20px;
    height: 30px;

    transform: translate(-10px, -30px);
  }
`;
